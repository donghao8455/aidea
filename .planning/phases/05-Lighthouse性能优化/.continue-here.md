---
context: phase
phase: 05-Lighthouse性能优化
task: 2
total_tasks: 5
status: paused
last_updated: 2026-07-18T07:08:50Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each constraint explicitly before proceeding.

- [ ] CONSTRAINT: Dev 环境热重载掩盖真实 CLS — `min-height` 修复在 dev 环境测试 CLS 仍=0.250，生产构建可能有不同结果。**必须用生产构建测试后再判断修复是否有效**
- [ ] CONSTRAINT: Windows Lighthouse Chrome Session 问题 — `Protocol error (Page.enable)` 导致 HTML 报告无法生成，JSON 结果需检查文件是否存在
- [ ] CONSTRAINT: Mobile Lighthouse 不支持 `--preset=mobile` — 需要用 `--form-factor=mobile --throttling-method=simulate` 替代

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Dev vs Prod 差异 | Dev 热重载掩盖真实性能问题，CLS 在 dev 未改善不代表生产无效 | blocking | 必须 `npm run build` + `serve build` 后再跑 Lighthouse |
| Lighthouse session 超时 | Windows 下 Chrome session 报 Protocol error 但 JSON 结果正常生成 | advisory | 添加 `--extra-headers='{"x-lh-internal": "prestart"}'` 参数绕过 |
| Effect 优先级冲突 | Effect 2/3 修改 opacity 覆盖 Effect 5（聚焦模式）的样式 | blocking | Effect 2/3 只负责 fill/stroke，Effect 5 统一管理 opacity |
| 搜索浮层挤压文档流 | `position:relative` 导致下拉列表占据文档流位置，推挤下方元素 | blocking | 浮层必须 `position:absolute`，父容器需有 `position:relative` |

**Do not proceed until all boxes are checked.**

<current_state>

**Phase 5: Lighthouse 性能优化 — 暂停审查**

本次会话转向首页交互逻辑审查与修复：
- 发现 8 个交互 bug，全部修复完成
- Dev server 在 3001 端口运行
- 所有修改已验证无编译错误

等待用户决策 Phase 5 下一步方向。

</current_state>

<completed_work>

Completed Tasks:
- Task 1: Lighthouse 基线审计 — Done
  - Desktop Performance=55, FCP=0.45s→0.25s, LCP=2.17s, TBT=489ms→506ms, CLS=0.250
  - Mobile 测试失败(Windows chrome session 问题)
- Task 2: 修复图片尺寸问题 (CLS) — Done
  - GraphCanvas container 加 min-height: 700px
  - AINewsSidebar 列表加 min-height: 200px
  - main 加 min-height: 1200px
- Task 3: 优化字体加载 — Done
  - Google Fonts preconnect 已添加（代码中无 Google Fonts 引用）
  - x6.min.js 已加 defer
- Task 6: 代码审查 (2026-06-17) — Done
  - 确认无严重 bug，理论风险均为低优先级
- Task 7: 首页交互逻辑修复 (2026-07-18) — Done
  - Escape 键退出聚焦（修复 no-op bug）
  - 搜索键盘导航（ArrowUp/Down/Enter，forwardRef 方案）
  - 搜索选择后清空搜索框
  - Effect 2/3 移除 opacity 设置，解决与聚焦模式冲突
  - 双击节点退出聚焦（400ms 双击检测）
  - 搜索下拉改为 position:absolute 浮层（内嵌 .searchBox）

</completed_work>

<remaining_work>

- Task 4: 优化 JavaScript bundle 大小 — 未开始
  - main.js = 990KB 是主要瓶颈
  - 需检查 fuse.js 是否可懒加载
  - 需检查 X6 初始化是否可以延迟
- Task 5: 最终 Lighthouse 验证 — 未开始
  - 需 `npm run build` + `serve build` 后跑 Lighthouse

</remaining_work>

<decisions_made>

- x6.min.js 加载策略: defer 不阻塞渲染
- preconnect 对无 Google Fonts 引用的项目无害，但保留
- CLS 根因: GraphCanvas 动态高度撑开 main 元素
- Social card 修复: social-card.png 不存在，改为 docusaurus-social-card.jpg
- CR-01 评估为低优先级: 数据来自静态源码，非用户输入
- CR-02 评估为技术债非bug: 代码使用 fallback 机制在 X6 v3 下行为正确
- Effect 2/3 只管 fill/stroke，不碰 opacity — 避免与 Effect 5 聚焦模式冲突
- 搜索浮层 position:absolute — 相对于 .searchBox（position:relative）定位
- SearchSuggest 内嵌 .searchBox — 共享 relative，确保浮层左对齐

</decisions_made>

<blockers>

- CLS 修复未在 dev 环境验证成功，可能需要生产构建才能看到效果
- TBT main.js=990KB 需要较大改动(code-splitting)，当前未处理
- Mobile Lighthouse 测试无法完成

</blockers>

## Required Reading (in order)

1. `.planning/phases/05-Lighthouse性能优化/05-01-SUMMARY.md` — 本次优化完整执行记录
2. `aides/docs/operations/lighthouse.md` — Lighthouse 运维指南

## Infrastructure State

- Dev server: `npm start` on port 3001 (运行中)
- 已修改文件已提交或待提交

## Critical Anti-Patterns (do NOT repeat these)

- **Effect 优先级冲突**: Effect 2/3 设置 opacity 覆盖 Effect 5 → Effect 2/3 只管 fill/stroke
- **搜索浮层占据文档流**: position:relative 的容器内元素会推挤下方 → 浮层改 position:absolute

<context>

Phase 5 Lighthouse 性能优化已暂停等待用户决策。本次会话恢复工作后转向首页交互 bug 修复，发现并修复了 8 个交互问题。

交互修复涉及的文件：
- aides/src/components/Graph/GraphCanvas.tsx (Effect 2/3 opacity 修复)
- aides/src/components/SearchSuggest/index.tsx (forwardRef 键盘导航)
- aides/src/components/SearchSuggest/styles.module.css (浮层定位)
- aides/src/pages/index.tsx (Escape键/双击/SearchSuggest 内嵌)

</context>

<next_action>

用户决策:
1. 继续 Phase 5 — 用生产构建验证 CLS 修复
2. 切换其他阶段 — 内容扩展（CONT）、移动端适配（MOBILE）、AI 资讯（NEWS）等
3. 暂停优化 — 等更好时机

</next_action>
